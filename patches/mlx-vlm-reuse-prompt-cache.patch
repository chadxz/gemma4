diff --git a/mlx_vlm/server.py b/mlx_vlm/server.py
--- a/mlx_vlm/server.py
+++ b/mlx_vlm/server.py
@@ -31,6 +31,7 @@ from .generate import (
     DEFAULT_THINKING_END_TOKEN,
     DEFAULT_THINKING_START_TOKEN,
     DEFAULT_TOP_P,
+    PromptCacheState,
     generate,
     normalize_resize_shape,
     stream_generate,
@@ -83,6 +84,15 @@ def get_max_kv_size(model: str):
 def get_quantized_kv_start():
     return int(os.environ.get("QUANTIZED_KV_START", DEFAULT_QUANTIZED_KV_START))
 
+
+def get_reuse_prompt_cache():
+    return os.environ.get("REUSE_PROMPT_CACHE", "false").lower() in (
+        "1",
+        "true",
+        "yes",
+        "on",
+    )
+
 
 @asynccontextmanager
 async def lifespan(app):
@@ -186,6 +196,7 @@ def get_cached_model(model_path: str, adapter_path: Optional[str] = None):
         "processor": processor,
         "config": config,
         "vision_cache": VisionFeatureCache(),
+        "prompt_cache_state": PromptCacheState() if get_reuse_prompt_cache() else None,
     }
 
     return model, processor, config
@@ -910,6 +921,7 @@ async def responses_endpoint(openai_request: OpenAIRequest):
                         prompt=formatted_prompt,
                         image=images,
                         vision_cache=model_cache.get("vision_cache"),
+                        prompt_cache_state=model_cache.get("prompt_cache_state"),
                         **generation_kwargs,
                     )
 
@@ -994,6 +1006,7 @@ async def responses_endpoint(openai_request: OpenAIRequest):
                     prompt=formatted_prompt,
                     image=images,
                     verbose=False,  # stats are passed in the response
+                    prompt_cache_state=model_cache.get("prompt_cache_state"),
                     **generation_kwargs,
                 )
                 # Clean up resources
@@ -1133,6 +1146,7 @@ async def chat_completions_endpoint(request: ChatRequest):
                         image=images,
                         audio=audio,
                         vision_cache=model_cache.get("vision_cache"),
+                        prompt_cache_state=model_cache.get("prompt_cache_state"),
                         **generation_kwargs,
                     )
 
@@ -1282,6 +1296,7 @@ async def chat_completions_endpoint(request: ChatRequest):
                     audio=audio,
                     verbose=False,  # Keep API output clean
                     vision_cache=model_cache.get("vision_cache"),
+                    prompt_cache_state=model_cache.get("prompt_cache_state"),
                     **generation_kwargs,
                 )
                 # Clean up resources
@@ -1491,6 +1506,14 @@ def main():
         help="Enable auto-reload on file changes (development only). "
         "WARNING: watches the entire working directory — can cause excessive memory "
         "usage with large models in repos with frequent file changes.",
+    )
+    parser.add_argument(
+        "--reuse-prompt-cache",
+        action="store_true",
+        default=False,
+        help="Reuse prompt KV cache across sequential requests when prompts share "
+        "a common prefix. Best for one active conversation at a time; increases "
+        "memory retained between requests.",
     )
     args = parser.parse_args()
     if args.trust_remote_code:
@@ -1505,6 +1528,7 @@ def main():
     os.environ["KV_QUANT_SCHEME"] = args.kv_quant_scheme
     os.environ["MAX_KV_SIZE"] = str(args.max_kv_size)
     os.environ["QUANTIZED_KV_START"] = str(args.quantized_kv_start)
+    os.environ["REUSE_PROMPT_CACHE"] = "true" if args.reuse_prompt_cache else "false"
 
     uvicorn.run(
         "mlx_vlm.server:app",
diff --git a/mlx_vlm/generate.py b/mlx_vlm/generate.py
--- a/mlx_vlm/generate.py
+++ b/mlx_vlm/generate.py
@@ -682,14 +682,36 @@ def stream_generate(
             if not has_image_in_new:
                 pixel_values = None
                 kwargs.pop("cached_image_features", None)
-            # Reuse the saved KV cache (trimmed to prefix length)
+            # Reuse the saved KV cache (trimmed to prefix length). Different
+            # cache implementations expose length/trim APIs differently, so
+            # prefer generic methods before falling back to direct slicing.
             kv_cache = prompt_cache_state.cache
-            # Trim cache to prefix_len in case it includes generated tokens
             for c in kv_cache:
-                if hasattr(c, "keys") and c.keys is not None:
-                    cached_len = c.keys.shape[2]
-                    if cached_len > prefix_len:
-                        c.keys = c.keys[:, :, :prefix_len, :]
-                        c.values = c.values[:, :, :prefix_len, :]
-                        if hasattr(c, "offset"):
-                            c.offset = prefix_len
+                cached_len = None
+                if hasattr(c, "size") and callable(c.size):
+                    try:
+                        cached_len = c.size()
+                    except TypeError:
+                        cached_len = None
+                if cached_len is None and hasattr(c, "offset"):
+                    cached_len = c.offset
+                if (
+                    cached_len is None
+                    and hasattr(c, "keys")
+                    and c.keys is not None
+                    and hasattr(c.keys, "shape")
+                ):
+                    cached_len = c.keys.shape[2]
+
+                if cached_len is None or cached_len <= prefix_len:
+                    continue
+
+                trim_amount = cached_len - prefix_len
+                if hasattr(c, "trim") and callable(c.trim):
+                    c.trim(trim_amount)
+                    continue
+
+                if (
+                    hasattr(c, "keys")
+                    and c.keys is not None
+                    and hasattr(c.keys, "shape")
+                ):
+                    c.keys = c.keys[:, :, :prefix_len, :]
+                    c.values = c.values[:, :, :prefix_len, :]
+                    if hasattr(c, "offset"):
+                        c.offset = prefix_len
             kwargs["prompt_cache"] = kv_cache
